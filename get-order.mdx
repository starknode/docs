---
title: "Get order by ID"
api: "GET /order/list/{order_id}"
description: "Fetch a single payment order that belongs to the authenticated tenant."
authMethod: "key"
---

<ParamField header="x-api-key" type="string" required placeholder="your-api-key">
  Your StarkNode API key. You can find it in your [dashboard](https://app.starknode.io).
</ParamField>

<ParamField path="order_id" type="string" required>
  `payment_orders.id` (CUID).
</ParamField>

<RequestExample>

```bash cURL
curl --request GET \
  --url https://api.starknode.io/order/list/clxxxxxxxxxxxxxxxxxxxxxxxx \
  --header 'x-api-key: <api_key>'
```

```javascript Node.js
const orderId = "clxxxxxxxxxxxxxxxxxxxxxxxx";
const response = await fetch(
  `https://api.starknode.io/api/order/list/${orderId}`,
  { headers: { "x-api-key": "<api_key>" } }
);
const data = await response.json();
```

```python Python
import requests

order_id = "clxxxxxxxxxxxxxxxxxxxxxxxx"
response = requests.get(
    f"https://api.starknode.io/api/order/list/{order_id}",
    headers={"x-api-key": "<api_key>"},
)
data = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 - Success
{
  "statusCode": 200,
  "success": true,
  "status": "order_found",
  "message": "Order found successfully.",
  "data": {
    "id": "clxxxxxxxxxxxxxxxxxxxxxxxx",
    "tenant_id": "...",
    "from_currency_symbol": "BRL",
    "amount_from": "100.00",
    "to_currency_symbol": "USDT",
    "on_ramp": false,
    "gateway_method": "direct",
    "active": true,
    "payment_session_link": "https://app.starknode.io/sessions/payment?code=...",
    "order_created_at": "2025-01-01T12:00:00.000Z",
    "order_updated_at": "2025-01-01T12:00:00.000Z"
  }
}
```

```json 404 - Not Found
{
  "statusCode": 404,
  "success": false,
  "status": "order_not_found",
  "message": "Order not found."
}
```

```json 401 - Unauthorized
{
  "statusCode": 401,
  "success": false,
  "status": "customer_not_logged",
  "message": "Customer not logged in."
}
```

</ResponseExample>

### Response

<ResponseField name="data" type="object" required>
  Full `payment_orders` record when `success` is `true` (includes JSON fields such as `order_items`, `split_payment_config`, `payment_method_allowed` when set).
</ResponseField>